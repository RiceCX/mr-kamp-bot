// Require env file so we dont have to load it in ever again..
require("dotenv").config();

require("./src/bot")();
// Any thing else to load goes down here

// TODO Add DB here... im too lazy to do it rn
